'use strict';

var util = require('util');
var common = require('./common');

module.exports = {
  traceInfo: traceInfo,
  traceUpdate: traceUpdate,
  tasksList: tasksList,
  inRequestList: inRequestList,
  outRequestList: outRequestList,
  resourceCallList: resourceCallList,
  eventList: eventList
};


function traceInfo(req, res) 
{
  var out =
  {};
  res.json(out);
}

function traceUpdate(req, res) 
{
  var out =
  {};
  res.json(out);
}

function tasksList(req, res) 
{
  var out =
  {};
  res.json(out);
}

function inRequestList(req, res) 
{
  var out =
  {};
  res.json(out);
}

function outRequestList(req, res) 
{
  var out =
  {};
  res.json(out);
}

function resourceCallList(req, res) 
{
  var out =
  {};
  res.json(out);
}

function eventList(req, res) 
{
  var out =
  {};
  res.json(out);
}






